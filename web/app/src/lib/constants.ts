// Константы, на которые опираются компоненты, перенесённые из fleety.
//
// Полный файл fleety сюда не годится: там полторы тысячи строк про автопарк —
// статусы машин, ОСАГО, страницы списков. К архиву созвонов это отношения не
// имеет, а один только `process.env` в его первой строке ломал сборку, потому
// что здесь нет ни Next, ни типов node.

// Масштаб интерфейса на этот браузер. Применяется CSS-свойством `zoom` на
// <html> — так же, как это делает Ctrl+колесо в Chrome: масштабируются и px, и
// rem, раскладка пересчитывается, а вместе с ней и всё, что вынесено порталами
// (модалки, выпадашки, тосты).
export const ZOOM_OPTIONS = [80, 90, 100, 110, 125, 150] as const;
export const DEFAULT_ZOOM = 100;
export const ZOOM_STORAGE_KEY = "ui:zoom";

// Потолок на длину строки поиска внутри выпадашек. Вменяемый запрос короче;
// вставленная простыня текста раздувает адрес запроса до отказа прокси, и
// браузер показывает пустую ошибку сети вместо внятного ответа.
export const MAX_SEARCH_QUERY_LEN = 128;
